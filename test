#include <stdio.h>
#include <stdlib.h>
int main() {
  char m[80];
  int i;
  printf("Введите строку: ");
  fgets(m,sizeof(m), stdin);
  for (int i = 0; i < 80; i++){
    if (m[i] == 'a'){
      m[i] = 'A';} 
    else if (m[i] == 'b'){
      m[i] = 'B';}
  }
  printf("%s\n", m);
  return 0;	
}
